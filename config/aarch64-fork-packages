# Package bases added only by the AArch64 fork.
#
# The integration test subtracts this explicit overlay from the complete scope
# and requires the remainder to match the upstream edge AArch64 range.

omarchy-aarch64-keyring
omarchy-aarch64-config
omarchy-spice-guest-tools

# Temporary official-version rebuild while ALARM's Hyprland requires the
# retired Aquamarine SONAME 13. Drop after ALARM publishes a compatible update.
hyprland
